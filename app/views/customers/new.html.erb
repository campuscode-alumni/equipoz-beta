<h1>Cadastrar Cliente</h1>
<%= simple_form_for @customer do |f| %>
  <%= f.input :name, label: 'Nome' %>
  <%= f.input :legal_name, label: 'Razão Social' %>
  <%= f.input :customer_type, label: 'Tipo de Cliente' %>
  <%= f.input :document, label: 'CPF/CNPJ'%>
  <%= f.input :fiscal_number, label: 'Inscrição Estadual'%>
  <%= f.input :contact_name, label: 'Contato'%>
  <%= f.input :phone_number, label: 'Telefone'%>
  <%= f.input :email, label: 'E-mail'%>
  <%= f.input :address, label: 'Endereço'%>
  <nav>    
    <%= f.button :submit, 'Cadastrar Cliente' %>
    <%= link_to 'Voltar', contracts_path, class: 'btn btn-default' %>
  </nav>
  <% end %>
